{
  "name": "libs/health",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "targets": {
    "clear": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["rimraf dist coverage"],
        "cwd": "libs/health"
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["prettier --write \"src/**/*.ts\""],
        "cwd": "libs/health"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["eslint \"src/**/*.ts\" --fix"],
        "cwd": "libs/health"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nest build"],
        "cwd": "libs/health"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nest start --watch"],
        "cwd": "libs/health",
        "envFile": "libs/health/.env.dev"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nest start"],
        "cwd": "libs/health"
      },
      "dependsOn": ["build"]
    },
    "start:prod": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["node dist/libs/health/src/main.js"],
        "cwd": "libs/health"
      },
      "dependsOn": ["build"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "options": {
        "jestConfig": "libs/health/jest.config.js",
        "verbose": true
      }
    },
    "test:watch": {
      "executor": "@nx/jest:jest",
      "options": {
        "jestConfig": "libs/health/jest.config.js",
        "watch": true
      }
    },
    "test:coverage": {
      "executor": "@nx/jest:jest",
      "options": {
        "codeCoverage": true,
        "jestConfig": "libs/health/jest.config.js"
      },
      "dependsOn": ["clear"]
    }
  }
}
